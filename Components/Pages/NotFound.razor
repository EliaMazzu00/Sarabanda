@page "/not-found"
@layout MainLayout

<PageTitle>Pagina non trovata — Sarabanda</PageTitle>

<div class="page-simple">
    <h1>🎵 Qui non c'è niente</h1>
    <p>
        La pagina che cerchi non esiste. Le schermate del gioco sono
        <code>/admin</code>, <code>/display</code> e <code>/remote</code>.
    </p>
    <a class="back-link" href="/">Torna al menu</a>
</div>
